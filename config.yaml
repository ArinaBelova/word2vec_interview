data:
  file_path: "data.txt"

model:
  embedding_dim: 50
  window_size: 2
  negative_samples: 5

training:
  epochs: 100
  learning_rate: 0.05
  log_interval: 10 

wandb:
  project: "word2vec"
  entity: null
  run_name: null
  tags: ["skip-gram", "numpy"]
